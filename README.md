Trying out tcp and channels

Start server once, and then as many clients as you want. The clients read the standard input and send the messages to the server, which then relays it to all the clients.

